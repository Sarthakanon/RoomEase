meta {
  name: Get Analytics Predictions
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/analytics/predictions
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

