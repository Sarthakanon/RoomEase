meta {
  name: Process Join Request
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/join-requests/{{joinRequestId}}/process
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

