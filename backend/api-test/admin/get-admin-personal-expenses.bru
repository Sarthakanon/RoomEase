meta {
  name: Get Admin Personal Expenses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/admin/personal-expenses
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:none {
  token: {{authToken}}
}

