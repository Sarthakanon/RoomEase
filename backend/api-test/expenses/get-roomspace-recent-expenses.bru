meta {
  name: Get Roomspace Recent Expenses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/roomspaces/{{roomspaceId}}/expenses/recent
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

