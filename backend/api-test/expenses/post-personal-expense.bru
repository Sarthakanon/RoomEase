meta {
  name: Create Personal Expense
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/personal-expenses
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

