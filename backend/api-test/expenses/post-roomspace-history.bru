meta {
  name: Create Roomspace History Entry
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/roomspaces/{{roomspaceId}}/history
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

