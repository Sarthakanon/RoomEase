meta {
  name: Get Pending Deletion Requests
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/deletion-requests/pending
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

