meta {
  name: Get Roomspace Balances
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/roomspaces/{{roomspaceId}}/balances
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

