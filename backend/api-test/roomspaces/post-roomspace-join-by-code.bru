meta {
  name: Join Roomspace By Code
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/roomspaces/code/{{inviteCode}}/join
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

