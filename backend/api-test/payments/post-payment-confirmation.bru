meta {
  name: Create Payment Confirmation
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/roomspaces/{{roomspaceId}}/payments/confirm
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

