meta {
  name: Delete Payment Notification
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/payment-notifications/{{notificationId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

