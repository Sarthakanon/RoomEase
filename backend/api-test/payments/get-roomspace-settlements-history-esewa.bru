meta {
  name: Get Roomspace Settlements History (eSewa)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/roomspaces/{{roomspaceId}}/settlements/history
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

