meta {
  name: Verify Subscription Payment (eSewa)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/payments/subscription/verify
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

