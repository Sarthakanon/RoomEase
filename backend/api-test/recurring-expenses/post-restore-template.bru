meta {
  name: Restore Recurring Template
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/recurring-expenses/{{templateId}}/restore
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: application/json
  Cookie: session_id={{sessionId}}
}

auth:bearer {
  token: {{authToken}}
}

